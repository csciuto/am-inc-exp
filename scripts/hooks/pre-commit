#!/bin/sh
# Tracked pre-commit hook. Enable it once per clone with:
#     git config core.hooksPath scripts/hooks
# Stamps docs/version.json (see scripts/stamp-version.sh) and stages it so the
# footer's build info travels with the commit.
set -eu

scripts/stamp-version.sh
git add docs/version.json
